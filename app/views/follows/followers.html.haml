#container
    %h1= 'These people are following you '+  @user.username
    %br
    %ul
        -Follow.where("following_id = #{@user.id}").each do |f|
            %li=link_to f.user.username, f.user
