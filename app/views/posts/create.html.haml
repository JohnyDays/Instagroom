%img{:src => "#{Rails.root.join('public', 'uploads',@uploaded_io.original_filename)}"}
%br
=link_to "Your Posts" user_posts_path(@currentuser)
=link_to "Post something else", new_user_post_path(@currentuser)
